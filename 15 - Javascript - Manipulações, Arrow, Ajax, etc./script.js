let nome = 'samara maia';

//let resuldado = nome.length; //quadar o resultado

let resuldado = nome.indexOf('s'); //sabe a posição do objet
console.log(nome.length)// quantidade de caractere navariavel nome